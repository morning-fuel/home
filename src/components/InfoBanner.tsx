const InfoBanner = () => {
  return (
    <section className="w-full bg-primary text-primary-foreground py-2 mt-[64px]">
      <div className="container-narrow px-5 text-center text-sm font-medium">
        🚚 All deliveries (one-time & subscription plans) start from 1st April
        morning.
      </div>
    </section>
  );
};

export default InfoBanner;
